# 编程英语复习页验收

最终结果：开发环境 15 个场景、生产产物 6 个核心场景通过；最终失败与浏览器运行错误均为 0。

- 开发地址：`http://localhost:5186/`。
- 生产地址：`http://localhost:5186/dist/`。浏览器实际加载并核对 `index-5A1VgT6D.js`；对应样式产物为 `index-Bvvebz40.css`。
- 所有浏览器操作使用新建的独立 Chrome context，没有读取或改动真实用户的浏览器数据。
- 使用此前真实界面课程产生的隔离 fixture：9 个已准入词、3 个学习中词；另加入 2 个旧 mastered 词验证兼容。为测试筛选，仅调整隔离记录的到期时间和薄弱状态。

通过的功能：

- 进入复习后直接看到 11 个已学词，释义、例句、状态与逐词练习可见；`reviewReadyAt=0` 的 3 个词不混入。
- 英文、中文、英文例句、中文例句搜索；全部、待复习、需要巩固、未到期筛选；无匹配结果和清空筛选。
- 单词练习只更新该词，筛选练习只使用筛选结果，批量每轮最多 5 词；点击前重新检查最新存储，空池不会打开练习。
- 切换四种主题保留练习题、选项顺序与已选答案；完成最后一个薄弱词后筛选结果变空，关闭练习把焦点放回搜索框。
- 收藏保持原记录；复习列表和共享词汇库的单词、例句按钮实际播放 MP3，捕获 `playing` 与大于 0 的时长。
- 场景复习入口可用；其草稿经过返回词表、打开词汇练习、主题切换、分区切换和刷新后仍保存，可以继续提交。日常英语复习界面保持原有行为。
- 空记录有课程入口；较长列表可以分批加载并滚动到末项。
- 共享词汇库仍显示 3,620 条，原搜索、收藏和点读可用。

四种风格分别在 1440、900、768、390 像素宽度验收：首屏无需滚动即可看到词条，未出现水平溢出；展开场景与滚动也通过。保存 32 张截图：

- 首屏：`review-{minimal,sketch,print,graffiti}-{1440,900,768,390}.png`
- 展开场景：`review-scenes-{minimal,sketch,print,graffiti}-{1440,900,768,390}.png`
- 主任务已人工查看桌面、手机和四种风格的代表截图。

主任务另外执行并通过：88 项相关逻辑测试、`npx tsc --noEmit`、`npm run build`、`npm run verify`。本浏览器任务没有重复运行这些检查。

可复跑脚本：`tools/test-review-page-browser.mjs`。结果文件：

- `browser-results.json`：开发主组 14 场景。
- `browser-results-shared-row.json`：共享词汇库补充 1 场景。
- `browser-results-production-admitted-English-single-word-last-weak-shared-row-scene-practice.json`：最终生产核心 6 场景，含实际加载 bundle URL。

早期场景续练脚本曾错误假设切回分区后必须点击“继续”，实际页面已自动恢复同一道题；定位判断修正后已复跑通过。目录中的该次失败截图属于脚本开发历史，没有未解决的产品问题。
