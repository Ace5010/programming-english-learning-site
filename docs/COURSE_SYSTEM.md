# 课程与自动复习

更新日期：2026-09-22。

## 当前使用方式

两个分区统一为“课程、复习、词汇库 / 表达库、收藏”。课程与练习册合并，课程负责初学及尚不稳定内容的练习；达到准入条件后的到期练习和主动巩固放在复习。顶部“收藏”直接查看各区原有收藏，支持独立搜索、逐条正常／慢速播放与取消收藏，不因浏览或收藏改变学习状态。

课程页只显示当前动态生成的一节。下一节在本轮结束后，依据历史信心和薄弱方向重新编排；未来的课名和单元名不提前展示。错误较多时多补旧内容、少学或暂不学新内容，学习顺利时推进下一范围。日常原 24 课和编程原 24 课现在都是教学材料范围，不决定实际课数；全部材料已介绍后，仍可生成补充学习节。

日常材料保留 83 条点读表达；编程材料覆盖 63 个核心词，以技术阅读为主。原作者题目、变式和新增候选题组成可抽取的题库，不要求按固定清单全部做完，也不规定每个词必须考几次。完整 3,620 词词库继续作为查询资源，没有强制课程化。

## 一节课怎样变化

`src/adaptiveLearning.ts` 从历史薄弱目标选取本节重点，再酌量搭配下一材料范围中的新目标。教学卡只展示本节相关的表达或词语与例句，中文讲解最多使用两个源范围。候选题的目标必须已介绍，或属于本节确认教学的新目标。

本轮只先确定当前题；每次检查并继续后，结合具体目标、实际能力、近期错误和刚出现过的内容，加权随机选择下一题，穿插不同目标及题面。低信心目标从有线索的识别开始，再过渡到语境或撤去提示的回忆；不会首次看到长词就强制盲写。日常可少量加入原有口语练习，自查不作为自动准入证据。

每轮有有限练习预算，困难时可少量延长，上限为 20 题；同轮不重复同一个题目 ID。预算结束后尚不稳定的内容留给后续动态课，不无限重试。实际完成节数由 `learning.rounds` 记录，材料范围的旧 `completedAt` 只表示相应教学目标已介绍，不代表目标已经稳定。

## 如何判断已学和进入复习

打开教学卡、查询、收藏或播放音频不会产生学习能力证据。确认教学后登记首次学习时间，状态为“正在学习”，此时不进入长期复习。之后按真实回答更新 `learning.targets`：提示和错误降低信心，识别题的增益较低；间隔后独立完成不同签名的语境或回忆题，才提供迁移证据。

达到信心、相关能力和间隔迁移条件后，目标获得 `readyAt`，再准入长期复习。一次答对不能准入，同一道题换 ID 或即时记住刚展示的答案也不能替代迁移。编程阅读目标需要阅读语境证据；日常简短表达可通过独立回忆形成迁移，不要求每条表达都有同一种题型。

词汇库区分尚未学习、正在学习和已经准入复习的内容。旧手动标记过的词是兼容例外：继续进入复习，并跳过全新词教学；原始标记和真实答题证据保留，不伪造分能力答案。页面移除手动掌握按钮，学习编排用的内部信心值不是对用户公布的单一掌握度。

编程词义、语境理解、拼写和听音识别分别记录。系统优先检查到期薄弱方向；同一天反复答对、看提示后改对或当场再检查不冒充延迟回忆。之后不同日期独立答对才延长该能力间隔；答错或提示缩短间隔。场景复习和词汇复习读取同一份四能力记录。日常英语沿用理解、听力、书写、口语记录，并按具体表达登记知识；口语自查不能证明自动评定的发音掌握。

长期复习再次出现困难时，相应学习目标降低信心并重新进入下一节的补学选择。已经取得的准入时间保留，避免内容从复习池消失；补学与长期复习可以同时照顾这个弱项。能力更新只发生在实际测试方向，未测试能力保持未测。

## 数据与兼容

| 记录 | 存储 |
| --- | --- |
| 日常课程、表达知识、表达收藏和自适应学习状态 | 原 `codewords-daily-v1`，version 1 可选扩展 |
| 编程课程、自适应学习状态和续学会话 | `codewords-programming-course-v1`，原格式可选扩展 |
| 编程词级四能力与复习时间 | 原 `codewords-review-v1`，保留旧词义和拼写记录 |
| 编程收藏与旧掌握标记 | 原键保持；旧掌握标记只读兼容 |

原 3,560 个词的 ID 均保留，新增 60 词固定追加。`learning`、会话 `adaptive` 及词级准入信息为可选扩展；旧完成记录、真实能力证据和旧会话不重置。历史完成本身只迁移为已介绍，不能凭空推出新的信心或迁移成绩；旧手动掌握的兼容准入单独处理。

课程答题同步以持久化凭据去重；提示即使尚未提交也保存，恢复时使用原始发生时间。计划、随机状态和已经选定的题目随会话保存，刷新不会重新抽当前题；草稿、提示、反馈和结果继续保留。损坏或未知版本记录停止写入并允许导出原文；存储失败保留本页进度。检测到另一页面修改时停止覆盖，不清空原数据。localStorage 不具备跨键事务，因此部分保存会明确提示。

## 设计原则与实现边界

本实现参考逐步增加挑战、依据错误调整练习和把回忆分散到不同时间的原则。相关公开说明见 Duolingo 的 [自适应课程介绍](https://blog.duolingo.com/keeping-you-at-the-frontier-of-learning-with-adaptive-lessons/) 与 [间隔重复介绍](https://blog.duolingo.com/spaced-repetition-for-learning/)。

这里实现的是一套可在本地检查、重放和测试的规则，不是 Duolingo 私有模型的复刻，也不宣称具备相同训练数据或效果。没有新增运行时 AI、后台服务或跨设备账户；自动测试证明规则与数据流程，不证明长期真实学习效果。

## 验证与边界

自动检查涵盖候选内容、动态编排、学习与复习准入、分能力证据、旧记录兼容、进度保存、提示恢复、重复同步、容错判题与变式目标。运行命令如下：

```powershell
npx tsc --noEmit
node --test tools/test-review.mjs tools/test-lesson.mjs tools/test-lesson-exercise.mjs tools/test-spelling.mjs tools/test-daily-content.mjs tools/test-daily-progress.mjs tools/test-daily-tolerance.mjs tools/test-written-answer.mjs tools/test-daily-speech-state.mjs tools/test-speech-comparison.mjs tools/test-course-loop.mjs tools/test-programming-content.mjs tools/test-programming-progress.mjs tools/test-programming-review.mjs
node --test tools/test-adaptive-learning.mjs tools/test-adaptive-admission.mjs tools/test-daily-practice.mjs tools/test-programming-practice.mjs
npm run verify
npm run build
node tools/test-adaptive-course-browser.mjs
```

当前动态阶段已通过 197 项自动测试、TypeScript 检查、生产构建与资源完整性检查。此计数是当前回归结果；后续变更增加测试时应重新记录实际结果，不把它当作固定验收数量。

动态课程阶段已在隔离 Chrome 中通过开发页面 18 个场景：两区分别覆盖顺利与困难表现，共 24 个动态节、408 次客观作答，并完成 1 次口语自查。构建成品另通过 5 个场景，包含 32 次客观作答和音频检查；其中最终 `index-DdbVP7DO.js` 通过两区从未完成复习确认切换到新课的回归。两组运行均无最终失败或浏览器错误。详见 [最终验收报告](../artifacts/adaptive-course/verification-summary.md)，报告与截图位于 `artifacts/adaptive-course/`。

浏览器验证使用独立测试上下文，不访问真实用户档案或修改用户学习记录。纯函数时间模拟与实际 UI 检查分别报告；本次口语自查不等于麦克风识别或发音质量验收。

### 保留的旧阶段证据

固定课程阶段曾通过 UI 完成两区 48 课、240 道主练并逐课刷新，验证四种主题在 1440 和 390 宽度下的三个页面及练习；覆盖迁移、续学、当时的到期/提前复习、坏 JSON、能力记录、收藏和实际 MP3 播放。原报告与截图位于 `artifacts/course-system/`，脚本为 `tools/test-course-system-browser.mjs`。这些是旧导航和固定课程阶段的证据，不能当作最终动态编排、延迟准入或弱项回流的验收。

该阶段新增及修改的 274 个 Aria / Guy 录音已完整解码验证，日常音频文本与文件哈希保持不变。音频解码不等于逐条人工发音审核。当时的跨日复习使用测试时钟验证，口语验收为 12 次“自己表达”自查，未测试实际麦克风识别；当时未提交、推送或发布。
